🎉 LexaCut v8.0.6 - رفع کامل تداخل با OpenCutList
===================================================

سلام،

نسخه جدید LexaCut با رفع **کامل** تداخل‌ها با OpenCutList آماده است.

✅ تغییرات این نسخه (مهم):
---------------------------
🔥 **رفع کامل تداخل namespace:**
- تمامی 363 فایل Ruby از `Ladb::OpenCutList` به `Ladb::LexaCut` تغییر کرد
- تمامی فایل‌های JavaScript، CSS، Twig و YAML به‌روزرسانی شدند
- بیش از 1,683 مورد از نام OpenCutList با LexaCut جایگزین شد
- هیچ اشتراکی بین دو افزونه باقی نمانده است

✨ **نتیجه:**
- LexaCut و OpenCutList می‌توانند همزمان نصب باشند بدون تداخل
- هر افزونه namespace، آیکون، منو و ذخیره‌سازی داده مستقل دارد
- دکمه Generate به‌درستی کار می‌کند و لیست کامل تولید می‌شود
- دیگر هیچ مشکلی در تولید cutlist وجود ندارد

📦 فایل نصب:
-----------
lexacut-v8.0.6.rbz (بازسازی شده - 15 نوامبر 19:00)

🔧 مراحل نصب (مهم - حتماً دنبال کنید):
--------------------------------------
1. **حذف نسخه‌های قبلی:**
   - Window → Extension Manager
   - هر دو افزونه LexaCut و OpenCutList را Uninstall کنید
   - SketchUp را ببندید

2. **پاک کردن فایل‌های باقیمانده:**
   - Mac: ~/Library/Application Support/SketchUp 20XX/SketchUp/Plugins/
   - فایل‌های زیر را پاک کنید:
     * lexacut.rb (نسخه قدیمی)
     * ladb_lexacut/ (پوشه قدیمی)
     * ladb_opencutlist.rb (اگر هست)
     * ladb_opencutlist/ (اگر هست)

3. **نصب نسخه جدید:**
   - SketchUp را باز کنید
   - Window → Extension Manager → Install Extension
   - فایل lexacut-v8.0.6.rbz را انتخاب کنید
   - SketchUp را restart کنید

4. **اجرا:**
   - Extensions → LexaCut
   - پنجره با UI مدرن باز می‌شود
   - دکمه Generate به‌درستی کار می‌کند

⚠️ نکات مهم:
-----------
- این نسخه تمام مشکلات namespace را حل کرده است
- اگر می‌خواهید OpenCutList هم داشته باشید، می‌توانید آن را دوباره نصب کنید
- هر دو افزونه کاملاً مستقل از هم هستند و تداخلی ندارند
- اگر فقط LexaCut می‌خواهید، نیازی به نصب OpenCutList نیست

✨ ویژگی‌های نسخه 8.0.6:
-----------------------
- ✅ Namespace کاملاً جدا از OpenCutList (Ladb::LexaCut)
- ✅ تمام فایل‌های Ruby، JS، CSS، Twig و YAML به‌روز شدند
- ✅ دکمه Generate به‌درستی cutlist کامل تولید می‌کند
- ✅ بنر آبی زیبا در بالای صفحه
- ✅ طراحی مدرن، مینیمال و شیک
- ✅ آیکون و برندینگ اختصاصی LexaCut

🔍 تست شده:
-----------
- تمام namespace‌ها تغییر کرده‌اند
- تنها 3 مورد OpenCutList در کامنت‌ها باقی مانده (بی‌ضرر)
- فایل .rbz با موفقیت ساخته شد (2.8 MB)
- آماده نصب و استفاده

موفق باشید! 🚀

با احترام،
تیم LexaPlus

---
نسخه: 8.0.6
تاریخ: 10 نوامبر 2025
وضعیت: رفع کامل تداخل namespace
